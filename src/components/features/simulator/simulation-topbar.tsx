'use client';

export default function SimulationTopBar() {
  return (
    <div className="h-12 border-b bg-white flex items-center justify-between px-4">
      {/* Empty for now — you’ll add new content later */}
    </div>
  );
}